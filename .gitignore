# Dependencies
node_modules/

# Environment — contains secrets, never commit
.env

# PaperMC server runtime files (only jar + yml configs are version-controlled)
server/server.properties
server/octiv-world/
server/octiv-world_nether/
server/octiv-world_the_end/
server/cache/
server/libraries/
server/logs/
server/plugins/
server/versions/
server/crash-reports/
server/generated/
server/bundler/
server/*.log
server/session.lock
server/banned-ips.json
server/banned-players.json
server/ops.json
server/usercache.json
server/whitelist.json
server/version_history.json
server/paper-1.21.1.jar.backup
server/paper-1.21.11.jar
server/config/DIM-1/
server/config/level.dat
server/config/level.dat_old
server/config/session.lock

# npm cache (CI artifact, not source)
npm-cache/

# OpenClaw runtime state (sessions, memory, auth — local only)
agent/.openclaw/
agent/sessions/
.openclaw/workspace/sessions/
.openclaw/workspace/memory/
.openclaw/workspace/.session-*
.openclaw/workspace/*.session

# macOS
.DS_Store

# Redis persistence
dump.rdb

# Obsidian (ignore entire directory)
.obsidian/
vault/

# Documentation (local only)
TXT/

# Claude Code worktrees (full repo copies, very heavy for git status)
.claude/worktrees/
.claude/settings.local.json

# MCP config (local only)
.mcp.json

# IDE settings
.vscode/
.smart-env/
.smtcmp_json_db/
.smtcmp_vector_db.tar.gz

# Session notes
2026-03-02.md
setup-obsidian-v3.3-final.sh
*.kanban.md
Untitled Kanban.md

# Generated files (local only)
*.docx
*.png
setup-claude.sh
claude_desktop_config.json

# Tools (local dev scripts)
tools/

# API key test files (never commit hardcoded keys)
test_gemini_api.js
check_telegram.js

# Bot config (contains secrets — local only)
config/bot-config.json

# Coverage reports
coverage/

# LSP / IDE tooling
.serena/

# npm
npm-debug.log*

# Logs
logs/
*.log
.vercel
.env*.local
.mcp.json.future
.openclaw/workspace/.openclaw/
