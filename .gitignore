# Dependencies
node_modules/

# Environment — contains secrets, never commit
.env

# PaperMC server runtime files (only jar is version-controlled)
server/server.properties
server/octiv-world/
server/cache/
server/libraries/
server/logs/
server/plugins/
server/versions/
server/crash-reports/
server/generated/
server/bundler/
server/*.log
server/session.lock

# OpenClaw runtime state
agent/.openclaw/
agent/sessions/

# macOS
.DS_Store

# Redis persistence
dump.rdb

# Obsidian (ignore entire directory)
.obsidian/
vault/

# Documentation (local only)
TXT/

# Claude Code worktrees (full repo copies, very heavy for git status)
.claude/worktrees/
.claude/settings.local.json

# MCP config (local only)
.mcp.json

# IDE settings
.vscode/
.smart-env/
.smtcmp_json_db/
.smtcmp_vector_db.tar.gz

# Session notes
2026-03-02.md
setup-obsidian-v3.3-final.sh
*.kanban.md
Untitled Kanban.md

# Generated files (local only)
*.docx
setup-claude.sh
claude_desktop_config.json

# npm
npm-debug.log*

# Logs
logs/
*.log
.vercel
.env*.local
